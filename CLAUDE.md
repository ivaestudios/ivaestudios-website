# IVAE Studios — Cloud-Native Site

Static site for [ivaestudios.com](https://ivaestudios.com) — luxury destination
photography in Cancún / Riviera Maya / Los Cabos. Bilingual (EN / ES).

> **Mobile is canonical.** 99% of IVAE clients view the site on phone.
> Mobile-first is the priority for any layout, copy, or audit work — not
> a responsive afterthought.

## ⚡ For any Claude session: read [`seo/AGENTS.md`](seo/AGENTS.md) first

A full enterprise SEO agent system is live. 11 autonomous agents run every
Monday 9:08 AM local time. **Critical context lives in `seo/AGENTS.md`** —
account hygiene, credentials, marker contracts, what's pending, etc. Do not
make changes to the SEO system without reading that file first.

**Critical accounts note:** the user has multiple Google accounts in Chrome.
Always use `vianeydm07@gmail.com` (Vianey, `authuser=1`) for all IVAE work.
Other accounts at `authuser=0` and `authuser=2` belong to unrelated projects
and must never be used here.

## Two brands under one site

The site hosts **two brands** that share the same domain, repo, and
deploy pipeline but have distinct visual identity, audience, and URL space:

| Brand | Audience | Visual identity | URL space |
|---|---|---|---|
| **IVAE Studios** (parent) | Luxury photography clients (couples, families, weddings) | Gold (`#c8a96a`), Cormorant Garamond + Syne, dark editorial | `/`, `/cancun.html`, `/riviera-maya.html`, `/about.html`, `/post-*.html` blog, `/es/` mirror |
| **IVAE Marketing** (sub-brand) | B2B social-media management clients (hotels, restaurants, spas, dental) | Purple/pink particles, lighter palette, distinct hero treatment | `/social-media-management.html` (master), 6 industry landings, 5 SMM blog posts, `/es/manejo-redes-sociales.html` |

IVAE Marketing pages link back to IVAE Studios as parent. The SMM master
page (EN + ES) carries a visible "sub-brand of IVAE Studios" statement and
parent-home link. The intake form for IVAE Marketing leads is
`/marketing-intake.html` → `functions/api/marketing-intake.js` →
Resend → `info@ivaestudios.com`.

## Hosting

- **Cloudflare Pages** auto-deploys on every push to `main`.
- Cloudflare reads `_redirects` and `_headers` at the repo root.
- `netlify.toml` is a historical artifact — **Cloudflare ignores it**, so do
  not put redirects or headers there.

## Repo layout

```
/                              → root HTML pages (index, about, blog, 404, etc.)
/es/                           → Spanish mirror
/blog/ (via redirect)          → post-*.html at the repo root are rewritten to /blog/<slug>
/images/                       → assets
/js/                           → small client scripts (lang-switcher, analytics, pwa-register, etc.)
/styles/                       → shared CSS
/scripts/                      → Python automation (runs in GitHub Actions, not in the browser)
/.github/workflows/            → CI
/functions/api/                → Cloudflare Pages Functions
  /functions/api/gallery/      → client gallery backend (catch-all worker)
  /functions/api/marketing-intake.js → IVAE Marketing form → Resend → info@ivaestudios.com
/gallery/                      → client gallery sub-app (static + worker)
/seo/                          → SEO agent system (see seo/AGENTS.md)
  /seo/playbooks/              → IVAE Marketing 90-day execution, press kit, directories, GBP, Wikipedia drafts, etc.
  /seo/reports/                → Weekly markdown reports written by the autonomous sweep
_redirects                     → Cloudflare redirect/rewrite rules
_headers                       → Cloudflare response headers (incl. CSP + HSTS, allowlists Turnstile + Resend)
sitemap.xml                    → 173 URL entries (regenerated by scripts/update_sitemap.py)
sitemap-ai.xml                 → 51 AI-priority URLs (IVAE Studios + IVAE Marketing landings)
robots.txt, llms.txt, llms-full.txt, humans.txt, ai.txt → AI discovery files
api/facts.json                 → Machine-readable facts (Wikidata Q-IDs, both brands)
sw.js                          → Service Worker (currently v9, 2026-05-26)
```

## Cloud-only workflow — the rules

1. **All work happens from git.** No local-machine dependency. Editing in
   GitHub.dev, Codespaces, or any Claude session with repo access is fine.
2. **Never commit secrets.** Anything in `.gitignore` is kept out of the repo
   on purpose. Secrets live in GitHub → Settings → Secrets and variables →
   Actions.
3. **Push to `main` = live in ~1 minute.** Cloudflare auto-deploys. The
   `SEO — Index URLs` workflow fires the same push and asks Google to recrawl
   the changed pages.

## GitHub Secrets required

| Secret | Required | What it's for |
|---|---|---|
| `GOOGLE_INDEXING_SA_JSON` | ✅ | Full JSON content of the Search Console service-account key. Used by `scripts/index_urls.py` to call the Google Indexing API. |
| `BING_INDEXNOW_KEY` | optional | For `seo-indexnow.yml` (already set) |
| `CLOUDFLARE_TOKEN` | optional | For `cf-purge.yml` cache purges |
| `IVAE_CRON_BEARER` | optional | For gallery cron sweep |
| `TURNSTILE_SECRET_KEY` | ⛔ pending | Cloudflare Turnstile server-side verification for marketing-intake form (see Owner action items) |
| `RESEND_API_KEY` | ⛔ pending | Resend API key for marketing-intake email pipeline (see Owner action items) |

## Workflows (`.github/workflows/`)

- **`SEO — Index URLs`** (`index-urls.yml`)
  - Auto-fires on push to `main` when any `*.html`, `sitemap.xml`, or
    `_redirects` changes.
  - Manual run: Actions → *SEO — Index URLs* → *Run workflow* — optionally paste
    a space-separated list of URLs, or leave blank for the default 21-URL set.
- **`SEO — Index IVAE Marketing URLs`** (`index-marketing-urls.yml`)
  - Same as above but scoped to the 21 IVAE Marketing URLs (master,
    6 industry landings, 5 SMM blog posts, ES mirrors).
- **`SEO — IndexNow`** (`seo-indexnow.yml`)
  - Bing IndexNow submission on every push to `main`.
- **`SEO — Link Audit`** (`link-audit.yml`)
  - Runs on every PR that touches HTML or `_redirects` (fails CI if any broken
    internal link is introduced).
  - Weekly schedule + manual trigger for report-only runs.
- **`SEO — Weekly Report`** (`seo-weekly-report.yml`)
  - Companion to the Monday 9:08 AM Claude scheduled sweep.
- **`SEO — Keyword Refresh`** (`seo-keyword-refresh.yml`)
  - GSC top-query pull + KW marker rotation.
- **`Optimize Images`** (`optimize-images.yml`)
  - On-demand image optimizer (AVIF + WebP).
- **`Cloudflare Purge`** (`cf-purge.yml`)
  - Targeted cache invalidation after deploys.
- **`Gallery Cron Sweep`** (`gallery-cron-sweep.yml`)
  - Daily 12:00 UTC, bearer-authenticated hit on `/api/gallery/admin/cron-sweep`.

## Recent audit waves (2026-05-26)

Three consecutive parallel-agent waves landed end-to-end fixes across the
whole site. Each wave shipped as a single commit; the second and third had
20 parallel audits running concurrently.

### Wave 1 — `cb4defa` — IVAE Marketing top-3 SEO push + enterprise audit fixes
**Scope:** launched the IVAE Marketing sub-brand (17 new HTML pages),
hardened Cloudflare (CSP, Functions backend), wired the intake form,
generated 14 execution playbooks under `seo/playbooks/`, and ran the first
audit wave (12 parallel agents).
- 6 EN industry landings + 6 ES mirrors + 5 SMM blog posts (~13K words total)
- Service JSON-LD + AI manifest expansion (llms.txt +20 triggers, llms-full.txt deep profile)
- `_headers` CSP allowlist for `challenges.cloudflare.com` + `api.resend.com`
- `functions/api/marketing-intake.js` hardened: origin-aware CORS, rate-limit
  fallback, Resend retry w/ exponential backoff, plain-text email, sanitized logs
- `scripts/index_marketing_urls.py` + 21 marketing URL set
- 100+ em-dashes removed across pillar pages + ES localization bug fixes

### Wave 2 — `02f29ff` — 20 parallel audits + critical fixes
**Scope:** site-wide health pass focused on production blockers, content
hygiene, and ES localization.
- **Critical:** `_redirects` was hijacking `/social-media-management` → ES, making
  the EN master unreachable. Removed the redirect, 6 alias rules now resolve correctly.
- `sw.js` bumped to v9 (removed dead `/offline.html` reference + duplicate mNav guard)
- `manifest.json` cleaned (removed nonexistent maskable icon ref)
- `social-media-management.html`: replaced undefined `--gold`/`--ink` CSS vars with hex
- 1,773 em-dashes removed in 43 ES blog posts (visible body only)
- ES localization bugs (Family ES FAQPage + BreadcrumbList were EN, About ES meta lang was English, etc.)
- 6 IVAE Studios pages now cross-link to `/social-media-management`
- 8 Vianey signature mentions converted to `/vianey-diaz` author links

### Wave 3 — `9125c91` — millimeter-perfection site-wide pass
**Scope:** the polish wave. Template-clone bugs, schema hardening,
performance attributes, WCAG, sitemaps, AI manifests.
- **REWRITES:** `/cenote-photographer-tulum.html` and `/mayakoba-wedding-photographer.html`
  were Cabo clones — wholesale rewritten with correct coords, 8 location-specific
  FAQs each, 3K+ words each
- 1,837 more em-dashes removed (48 EN blog posts) — total cleanup ~3,800 dashes across all 3 waves
- **Schema hardening:** 845 JSON-LD blocks audited, AggregateRating unified
  5.0/42 across 74 instances, `vianey-diaz` Person `@id` consistent across 268 refs
- **Performance:** 894 `decoding="async"` added, 99 width/height attributes
  added (CLS), 58 below-fold `loading="lazy"`, 16 titles trimmed ≤60ch, 42 descriptions ≤160ch
- **Sitemaps:** `sitemap.xml` 164 → 173 entries (12 previews removed, 21 missing added);
  `sitemap-ai.xml` 39 → 51 entries
- **E-E-A-T:** 198 Vianey author-link instances across 99 blog posts (top byline + bottom box, `rel="author"`)
- **Footer harmonization:** 125 files modified, 361 links added (Privacy + A11y + sub-brand)
- **WCAG 2.1 AA:** `--text-on-dark-3` opacity 0.34 → 0.55 (4.6:1 contrast), focus rings on 6 marketing pages, touch targets ≥44-48px
- **Forms (11 total):** privacy notice + honeypot + aria-required + aria-live on all
- **`audit_links.py`: 0 broken** (was 15 → 9 → 0 across waves)

## Owner action items — Tier 0 blockers (need real data from Vianey)

These are the hard blockers that no agent can resolve without human input
or external account access. Listed in priority order:

1. **Phone number — RESOLVED (2026-06-05).** Both numbers are REAL, confirmed by
   Vianey: `+52 228 857 0584` = phone CALLS (this is the Google Business Profile
   number) and `+52 990 204 6514` = WhatsApp. Schema `telephone` is now the CALL
   number (matches GBP for NAP) on 165 pages; WhatsApp stays in all wa.me links + a
   ContactPoint. DO NOT treat either number as fake or "invalid". The 4 owner-protected
   pages still carry the old WhatsApp number in their #organization telephone, pending
   owner OK to edit their schema.
2. **GBP coordinates / single canonical address.** Schema uses Cancún
   (`21.1619, -86.8515`) but the GBP pin may be in Riviera Maya
   (`20.4785722, -87.0756298`). Pick one canonical address (street + lat/lng)
   and we propagate.
3. **Cloudflare Turnstile sitekey.** `marketing-intake.html` currently has
   the placeholder `0x4AAAAAAA_PLACEHOLDER_REPLACE_IN_DASHBOARD`. Go to
   Cloudflare → Turnstile → create a widget for `ivaestudios.com`, paste
   the real sitekey here, and add the secret as `TURNSTILE_SECRET_KEY` in
   GitHub Secrets + Cloudflare Pages env.
4. **Resend domain verification + API key.** The marketing-intake form
   sends via Resend to `info@ivaestudios.com`. Verify `ivaestudios.com`
   in Resend (DKIM + SPF + DMARC), generate an API key, store as
   `RESEND_API_KEY` in GitHub Secrets + Cloudflare Pages env.
5. **Real testimonials — IN PROGRESS (2026-06-05).** IVAE has 47 REAL 5-star Google
   reviews (5.0 average). Three captured + added as visible testimonials + Review
   JSON-LD on the weddings/family/couples pillars: Joni Blake, Danielle Prager, Angila
   Golik. Placeholder names (Whitfield/Benedetti/Raghavan, plus Voss/Carter/Marin/
   Calderon/etc) were removed from all non-protected pages. STILL TODO: placeholders
   remain on the owner-protected `index.html` + `es/index.html` (need owner OK), and the
   other 44 real reviews need pulling from GBP (Google blocks bulk auto-extraction —
   collect via screenshots) + diversifying across pages.
6. **Real session pricing.** FAQ schema currently shows `$650–$3,500+`
   placeholder. Replace with real session rates so `Offer.price` +
   `priceValidUntil` schema can ship.

### Lower priority (Vianey may want to act on these)

- `AggregateRating` — RESOLVED (2026-06-05): the real GBP rating is **5.0 / 47
  reviews**, set on the #organization node site-wide. (Was a placeholder 5.0/42.)
- No street address in `Organization` schema yet (waiting on #2 above).
- Separate Google Business Profile for IVAE Marketing (sub-brand).
- Pricing blocks on `cancun.html` + `riviera-maya.html` (placeholder).
- `/vianey-diaz/` already shipped as `/vianey-diaz.html` (E-E-A-T) — kept here for reference.

## Useful commands

```bash
# Run the link audit locally (no credentials needed):
python scripts/audit_links.py --fail-on-broken

# Submit a custom URL list to Google from your machine
# (only works if GOOGLE_INDEXING_SA_JSON or GOOGLE_INDEXING_SA_FILE is set):
python scripts/index_urls.py https://ivaestudios.com/ https://ivaestudios.com/es/

# Submit the IVAE Marketing URL set:
python scripts/index_marketing_urls.py
```

## Gallery sub-app (added Apr 2026)

The client gallery (formerly the standalone Worker at
`gallery.ivaestudios.com`) now lives in this same repo:

- **Static assets**: `gallery/` (HTML, JS, CSS, images, sw.js)
- **API**: `functions/api/gallery/[[path]].js` (Cloudflare Pages Functions
  catch-all that re-uses all the original Worker routing logic — only the
  `/api/gallery` URL prefix gets stripped before the existing matchers run).
- **Cron**: `.github/workflows/gallery-cron-sweep.yml` (daily 12:00 UTC,
  calls a bearer-authenticated `/api/gallery/admin/cron-sweep` endpoint
  because Pages Functions has no scheduled trigger of its own).
- **Database**: Cloudflare D1 (`ivae-gallery-db`) with schema in
  `gallery/schema.sql` and migrations in `gallery/migrations/`.
- **Storage**: Cloudflare R2 bucket (`ivae-gallery-photos`).

See [`gallery/README.md`](gallery/README.md) for the full URL map, binding
config, secret list, and migration commands.

The standalone repo `ivaestudios/ivae-gallery` is now archived; everything
lives here.
